FLASK_SECRET_KEY=replace-with-a-long-random-secret
PORT=5000
FLASK_DEBUG=0
DATABASE_PATH=./rivals_stats.db

# Turso (cloud SQLite) — set these on Vercel for persistent storage
# Leave blank to use a local SQLite file instead
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=